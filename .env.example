# ── Vite (frontend) — required for Supabase in production ──────────────────
# Get anon key: Supabase Dashboard → Project Settings → API → anon public
VITE_SUPABASE_URL=https://fyvhajhhqbwfvqqsdeyx.supabase.co
VITE_SUPABASE_ANON_KEY=

# ── Prisma (migrations only — not bundled in the browser) ───────────────────
# Password must be URL-encoded if it contains special characters ($ & * etc.)
