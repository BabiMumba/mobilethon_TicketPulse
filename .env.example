# Supabase connection (optional).
# When these are set the app uses a real Supabase project for auth + data;
# when unset it transparently falls back to an in-memory / localStorage mock
# backend so the app is fully runnable offline for local development.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
